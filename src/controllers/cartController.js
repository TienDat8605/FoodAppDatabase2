const Cart = require('../models/Cart');

const handleGetCartItems = async (req, res) => {
    //TODO(get cart items for authenticated user)
}

const handleAddItemToCart = async (req, res) => {
    //TODO(add item to cart for authenticated user)
}

const handleUpdateItemInCart = async (req, res) => {
    //TODO(update item in cart for authenticated user)
}

const handleRemoveItemFromCart = async (req, res) => {
    //TODO(remove item from cart for authenticated user)
}

const handleClearCart = async (req, res) => {
    //TODO(clear cart for authenticated user)
}

const handleCheckoutCart = async (req, res) => {
    //TODO(checkout cart for authenticated user)
}

module.exports = {
    handleGetCartItems,
    handleAddItemToCart,
    handleUpdateItemInCart,
    handleRemoveItemFromCart,
    handleClearCart,
    handleCheckoutCart
};